# 🌐 دليل ترجمة المشاريع التلقائية
## نظام ترجمة كامل لمشاريع React/JSX باستخدام Python و Claude API

---

## 📦 المتطلبات الأساسية

### 1. البرامج المطلوبة:
- ✅ **Python 3.8+** - [تحميل](https://www.python.org/downloads/)
- ✅ **pip** (يأتي مع Python)
- ✅ **Claude API Key** - [الحصول عليه](https://console.anthropic.com/)

### 2. تثبيت المكتبات:
```bash
pip install anthropic
```

### 3. الملفات المطلوبة:
- `extract_texts.py` - استخراج النصوص من الكود
- `translate_texts.py` - ترجمة النصوص
- `check_progress.py` - فحص التقدم
- `split_translations.py` - تقسيم الملفات (اختياري)

---

## 🚀 الاستخدام السريع

### الخطوة 1️⃣: استخراج النصوص
```bash
python extract_texts.py
```

**الناتج:**
```
✅ تم استخراج 754 نص
✅ تم حفظ: translations_extracted.json
```

---

### الخطوة 2️⃣: إعداد API Key
```bash
# Windows PowerShell:
$env:ANTHROPIC_API_KEY="sk-ant-api03-xxxxxxxxxxxxx"

# Linux/Mac:
export ANTHROPIC_API_KEY="sk-ant-api03-xxxxxxxxxxxxx"

# تحقق من الإعداد:
echo $env:ANTHROPIC_API_KEY  # Windows
echo $ANTHROPIC_API_KEY      # Linux/Mac
```

---

### الخطوة 3️⃣: الترجمة
```bash
python translate_texts.py
```

**الناتج:**
```
🌐 بدء الترجمة (754 نص)...
✅ تمت ترجمة 200 نص
⚠️ تم الوصول للحد الأقصى
✅ تم حفظ النتائج
```

---

### الخطوة 4️⃣: فحص التقدم
```bash
python check_progress.py
```

**الناتج:**
```
📊 تقدم الترجمة:
إجمالي النصوص: 754
✅ مترجم: 200
⏳ يحتاج ترجمة: 554
📈 النسبة: 26.5%
```

---

### الخطوة 5️⃣: تكرار الترجمة
```bash
# كرر حتى تصل إلى 100%
python translate_texts.py
python check_progress.py
python translate_texts.py
python check_progress.py
# ... استمر حتى 100%
```

---

### الخطوة 6️⃣: تقسيم الملفات (اختياري)
```bash
python split_translations.py
```

**الناتج:**
```
✅ تم التقسيم إلى:
src/locales/ar.js
src/locales/en.js
src/locales/fr.js
src/locales/zh.js
```

---

## ⚙️ الإعدادات والتخصيص

### تغيير مجلد الصفحات:
افتح `extract_texts.py` وعدّل السطر 15:
```python
PAGES_DIR = "src/pages"  # غيّره حسب مشروعك
```

### تغيير عدد الترجمات في كل تشغيل:
افتح `translate_texts.py` وعدّل السطر 142:
```python
translated_data = translate_batch(data, max_translations=200)
# غيّر 200 إلى 50 أو 100 أو 300 حسب سرعة اتصالك
```

### إضافة لغات جديدة:
في `translate_texts.py`، أضف لغة جديدة:
```python
# مثال: إضافة الإسبانية (es)
if not item['es']:
    item['es'] = translate_text(source_text, 'ar', 'es')
    time.sleep(1)
```

وفي `split_translations.py`:
```python
languages = {
    'ar': 'العربية',
    'en': 'English',
    'fr': 'Français',
    'zh': '中文',
    'es': 'Español'  # أضف هنا
}
```

---

## 💰 تقدير التكاليف

### Claude API - Sonnet 4:
- **السعر:** $3 لكل مليون توكن إدخال
- **السعر:** $15 لكل مليون توكن إخراج

### أمثلة واقعية:

| حجم المشروع | عدد النصوص | التكلفة المتوقعة |
|-------------|-------------|-------------------|
| **صغير** | 100-300 نص | $0.20 - $0.50 |
| **متوسط** | 300-800 نص | $0.50 - $1.50 |
| **كبير** | 800-2000 نص | $1.50 - $4.00 |
| **ضخم** | 2000+ نص | $4.00+ |

**ملاحظة:** التكلفة تعتمد على:
- طول النصوص
- عدد اللغات المستهدفة (3 لغات = 3× التكلفة)
- عدد الأخطاء والإعادات

---

## 📁 هيكل الملفات

### قبل الترجمة:
```
project/
├── src/
│   └── pages/
│       ├── Home.jsx
│       ├── About.jsx
│       └── Contact.jsx
├── extract_texts.py
├── translate_texts.py
├── check_progress.py
└── split_translations.py
```

### أثناء الترجمة:
```
project/
├── translations_extracted.json  ← النصوص المستخرجة
├── translations_final.json      ← الترجمات المحفوظة
└── translations_GENERATED.jsx   ← الملف النهائي
```

### بعد التقسيم:
```
project/
└── src/
    └── locales/
        ├── ar.js     ← عربي فقط
        ├── en.js     ← إنجليزي فقط
        ├── fr.js     ← فرنسي فقط
        ├── zh.js     ← صيني فقط
        └── index.js  ← يجمعهم
```

---

## 🔧 حل المشاكل الشائعة

### ❌ خطأ: "Connection error"
**السبب:** طلبات كثيرة بسرعة (Rate limiting)

**الحل:**
1. انتظر دقيقة
2. قلل عدد الترجمات: `max_translations=100`
3. زِد وقت الانتظار: `time.sleep(2)`

---

### ❌ خطأ: "API Key not set"
**السبب:** لم يتم تعيين المتغير البيئي

**الحل:**
```bash
# تأكد من الإعداد
echo $env:ANTHROPIC_API_KEY

# إذا فارغ، أعد الإعداد
$env:ANTHROPIC_API_KEY="sk-ant-api03-xxxxx"
```

---

### ❌ الترجمة بطيئة جداً
**الحل:**
1. زِد عدد الترجمات: `max_translations=200`
2. قلل وقت الانتظار: `time.sleep(0.5)`
3. لكن احذر من Rate limiting!

---

### ❌ تُرجمت الآيات القرآنية
**الحل:**
استخدم النسخة `translate_texts_v5_NO_QURAN.py`

تحتوي على فلترة تلقائية للآيات.

---

## 🎯 أفضل الممارسات

### ✅ افعل:
- استخدم API Key خاص لكل مشروع
- راقب الرصيد باستمرار
- احفظ نسخة احتياطية من `translations_final.json`
- اختبر على صفحة واحدة أولاً
- راجع الترجمات قبل النشر

### ❌ لا تفعل:
- لا توقف السكريبت في منتصف التشغيل
- لا تشارك API Key علناً
- لا تترجم ملفات النظام (`node_modules`, `.git`)
- لا تستخدم Rate limiting عالي جداً

---

## 📊 مراقبة الاستخدام

### تحقق من الرصيد:
https://console.anthropic.com/settings/billing

### راجع الاستخدام:
https://console.anthropic.com/settings/usage

### قواعد الأسعار:
https://www.anthropic.com/pricing

---

## 🔄 استخدام في مشروع جديد

### خطوات سريعة:

```bash
# 1. انسخ السكريبتات
cd /path/to/new-project
cp /path/to/old-project/*.py .

# 2. عدّل الإعدادات (اختياري)
# افتح extract_texts.py وغيّر PAGES_DIR

# 3. شغّل الاستخراج
python extract_texts.py

# 4. ضع API Key
$env:ANTHROPIC_API_KEY="sk-ant-xxxxx"

# 5. ابدأ الترجمة
python translate_texts.py

# 6. راقب التقدم
python check_progress.py

# 7. كرر حتى 100%
```

---

## 🌍 اللغات المدعومة

### اللغات الحالية:
- 🇸🇦 العربية (ar)
- 🇬🇧 الإنجليزية (en)
- 🇫🇷 الفرنسية (fr)
- 🇨🇳 الصينية المبسطة (zh)

### يمكن إضافة:
- 🇪🇸 الإسبانية (es)
- 🇩🇪 الألمانية (de)
- 🇮🇹 الإيطالية (it)
- 🇯🇵 اليابانية (ja)
- 🇰🇷 الكورية (ko)
- 🇷🇺 الروسية (ru)
- 🇹🇷 التركية (tr)
- 🇵🇹 البرتغالية (pt)

---

## 📚 مصادر إضافية

### توثيق Claude API:
https://docs.anthropic.com/

### أفضل ممارسات الترجمة:
https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/

### دعم فني:
https://support.anthropic.com/

---

## ✨ ميزات إضافية

### 🔔 صوت تنبيه عند الانتهاء
السكريبت يصدر 3 أصوات "بيب" عند اكتمال الترجمة

### 🛡️ حماية الآيات القرآنية
تتخطى تلقائياً النصوص ذات التشكيل الكثيف

### 📈 تقارير مفصلة
احصائيات كاملة عن التقدم والنصوص

### 🔄 استئناف تلقائي
لا تحتاج البدء من الصفر عند إعادة التشغيل

---

## 🎓 نصائح للمبتدئين

### 1. ابدأ صغيراً:
جرب على مشروع صغير (100-200 نص) أولاً

### 2. راقب التكلفة:
افحص الرصيد بعد كل تشغيل

### 3. احتفظ بنسخ احتياطية:
انسخ `translations_final.json` بعد كل جلسة

### 4. اختبر الترجمات:
راجع بعض النصوص عشوائياً للتأكد من الجودة

### 5. استخدم التقسيم:
للمشاريع الكبيرة (+2000 سطر)

---

## 📞 الدعم

### إذا واجهت مشكلة:

1. **راجع قسم حل المشاكل** أعلاه
2. **تحقق من:**
   - الرصيد كافي؟
   - API Key صحيح؟
   - الاتصال بالإنترنت؟
3. **أعد المحاولة** بعد دقيقة
4. **قلل السرعة** إذا استمرت المشاكل

---

## 🏆 قصص نجاح

### مشروع alnourway:
- **754 نص** تمت ترجمتهم
- **4 لغات** (عربي، إنجليزي، فرنسي، صيني)
- **التكلفة:** ~$3.20
- **الوقت:** ~2 ساعة

---

## 📝 قائمة التحقق النهائية

قبل البدء:
- [ ] Python مثبت
- [ ] pip مثبت
- [ ] anthropic library مثبتة
- [ ] Claude API Key جاهز
- [ ] الرصيد كافي ($5+)

أثناء العمل:
- [ ] استخرجت النصوص
- [ ] ضبطت API Key
- [ ] بدأت الترجمة
- [ ] راقبت التقدم
- [ ] كررت حتى 100%

بعد الانتهاء:
- [ ] راجعت الترجمات
- [ ] قسمت الملفات (اختياري)
- [ ] نسخت للمشروع
- [ ] اختبرت الموقع
- [ ] نشرت التغييرات

---

## 🎉 تهانينا!

أصبح لديك الآن نظام ترجمة تلقائي كامل!

**يمكنك استخدامه في:**
- ✅ مشاريع React/Next.js
- ✅ مواقع متعددة اللغات
- ✅ تطبيقات الويب
- ✅ أي مشروع JSX/JavaScript

---

**صُنع بـ ❤️ باستخدام Claude API**

**تاريخ الإنشاء:** ديسمبر 2024
**الإصدار:** 1.0
